using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ZNote : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }



    /**
     * Staying in bounds
     * 
     * Can do this by 
     * 
     * 
     * Make some variable for radius and just use that for the collisions 
     * 
     * 
     */
}
